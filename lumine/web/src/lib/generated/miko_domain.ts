// Code generated by tygo. DO NOT EDIT.

//////////
// source: job.go

export type JobStatus = string;
export const JobStatusQueued: JobStatus = "queued";
export const JobStatusRunning: JobStatus = "running";
export const JobStatusSuccess: JobStatus = "success";
export const JobStatusFailed: JobStatus = "failed";
export const JobStatusCancelled: JobStatus = "cancelled";
export interface BuildRequest {
  repo: string;
  arch: string;
  /**
   * Mutually exclusive with Pkgbuild; exactly one source must be provided.
   */
  git?: GitSource;
  /**
   * Pkgbuild is the raw PKGBUILD contents.
   */
  pkgbuild?: string;
  /**
   * Files are extra filename->contents written alongside the Pkgbuild source.
   */
  files?: { [key: string]: string};
  /**
   * InstallPkgs are local package files installed before building.
   */
  install_pkgs: string[];
  /**
   * SignMode selects where signing happens: "host" (default) signs on the
   * worker with its host key; "client" leaves the artifact unsigned for the
   * requester to download and sign locally.
   */
  sign_mode?: string;
  /**
   * Timeout in minutes; 0 uses the server default.
   */
  timeout?: number /* int */;
}
export const SignHost = "host";
export const SignClient = "client";
/**
 * GitSource describes a git/AUR repository to clone as the build source.
 */
export interface GitSource {
  url: string;
  ref?: string;
  subdir?: string;
}
export interface BuildJob {
  id: string;
  repo: string;
  arch: string;
  status: JobStatus;
  logs: string;
  err?: string;
  packages?: string[];
  retries?: number /* int */;
  created_at: string;
  started_at?: string;
  ended_at?: string;
}
export interface BuildStats {
  workers: number /* int */;
  queue_length: number /* int */;
  running: number /* int */;
  counts: { [key: JobStatus]: number /* int */};
  total: number /* int */;
  success_rate: number /* float64 */; // success/(success+failed); 0 when none finished
  uptime_sec: number /* int */;
}
