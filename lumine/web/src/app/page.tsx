import { ScopeHero } from "@/components/console-scope-hero";

// 検索 (Search) category. A calm entry screen only: scope selectors + one
// dominant search box that navigates to /packages on submit.
export default function Home() {
    return <ScopeHero />;
}
