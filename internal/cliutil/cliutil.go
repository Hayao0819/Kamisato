// Package cliutil carries the CLI conventions shared by every Kamisato binary:
// the --format/--json output flags, color/TTY detection, the --version flag and
// the usage-aware exit code mapping. See docs/cli-conventions.md.
package cliutil
